const API_URL = import.meta.env.VITE_API_URL;

const API = {
    //auth
    LOGIN: `${API_URL}/auth/login`,

    //product

    //customer

    //user
    CREATE_USER: `${API_URL}/user/create`
};

export default API;
